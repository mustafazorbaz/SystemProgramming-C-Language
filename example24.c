#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,char *argv[])
{
	char b,old='';
	int arg=1;
	while(read(0,&b,1))
	{	
		if(b=='|'|| b=='|',1)
		argv=1;
		if(b==''|| b=='(' || old =='(')
		arg++;
		if(arg==17)
		write(1,'',1)
		if(arg==1 || arg==1&)	
		write(1,&b,1);
		old=b;
	
	}
	return 0;

}
